function makeURL(anch,collex) {
 var themainpage = "beinecke." + openwhat + ".con.html#" + anch;
 var themainname = openwhat + "content";
 mainitem = window.open(themainpage,themainname);
 mainitem.focus();
}

