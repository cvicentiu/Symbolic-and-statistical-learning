var xe_random; if (isNaN(document.axel)) {document.axel = Math.random() + ""; xe_random = document.axel * 1000000000000000000;}
NAD_xevals.push( 'xe_agt=' + escape(navigator.userAgent.toLowerCase()) );
NAD_xevals.push( 'xe_random=' + xe_random );
var mep1=''; 
while( NAD_xevals.length>0 ) { var pair = NAD_xevals.shift(); if( pair != null && pair != '' ) { mep1 = mep1.concat( "&" + pair ); }}
var msg=""; 
if((document.referrer)&&(document.referrer!="[unknown origin]")) 
{ 
	if(document.referrer.indexOf("?") == -1) 
	{	
		if(mep1.indexOf("RefDocLoc") == -1) 
		{
			msg+="&tax23_RefDocLoc="+escape(document.referrer.toString()); 
		}
	} 
	else 
	{ 
		var rdl=document.referrer; 
		var rdl1=rdl.indexOf("?"); 
		var rdl2=rdl.substring(0,rdl1); 
		if(mep1.indexOf("RefDocLoc") == -1) 
		{
			msg+="&tax23_RefDocLoc="+escape(document.referrer.toString()); 
		}
		
		if ( (rdl.indexOf("search.msn.com") != -1 ) || (rdl.indexOf("altavista") != -1) || (rdl.indexOf("google") != -1) ) 
		{ 
			var rdlLength=rdl.length; 
			var qS = rdl.substring(rdl1+1,rdlLength); 
			var pairs = qS.split("&"); 
			for (var i = 0; i < pairs.length; i++) 
			{ 	
				if (pairs[i].indexOf("q=") == 0 ) 
				{ 
					var pL=pairs[i].length; 
					var sekw = pairs[i].substring(2,pL); 
					msg+="&if_nt_SearchEngine_KW=" + sekw.toUpperCase();
				}
			}
		}			
		else if (rdl.indexOf(".yahoo.com") != -1) 
		{ 
			var rdlLength=rdl.length; 
			var qS = rdl.substring(rdl1+1,rdlLength); 
			var pairs = qS.split("&"); 
			for (var i = 0; i < pairs.length; i++) 
			{ 
				if (pairs[i].indexOf("p=") == 0 ) 
				{ 
					var pL=pairs[i].length; 
					var sekw = pairs[i].substring(2,pL); 
					msg+="&if_nt_SearchEngine_KW=" + sekw.toUpperCase();
				} 
			} 
		} 
		else if ((rdl.indexOf("aolsearch.aol.com") != -1) || (rdl.indexOf("lycos") != -1) ) 
		{ 
			var rdlLength=rdl.length; 
			var qS = rdl.substring(rdl1+1,rdlLength); 
			var pairs = qS.split("&"); 
			for (var i = 0; i < pairs.length; i++) 
			{ 
				if (pairs[i].indexOf("query=") == 0 ) 
				{ 
					var pL=pairs[i].length; 
					var sekw = pairs[i].substring(6,pL); 
					msg+="&if_nt_SearchEngine_KW=" + sekw.toUpperCase();
				} 
			} 
		} 
		else if (rdl.indexOf("looksmart") != -1) 
		{ 
			var rdlLength=rdl.length; 
			var qS = rdl.substring(rdl1+1,rdlLength); 
			var pairs = qS.split("&"); 
			for (var i = 0; i < pairs.length; i++) 
			{ 
				if (pairs[i].indexOf("key=") == 0 ) 
				{ 
					var pL=pairs[i].length; 
					var sekw = pairs[i].substring(4,pL); 
					msg+="&if_nt_SearchEngine_KW=" + sekw.toUpperCase(); 
				} 
			} 
		} 
		else if (rdl.indexOf("netscape") != -1) 
		{ 
			var rdlLength=rdl.length; 
			var qS = rdl.substring(rdl1+1,rdlLength); 
			var pairs = qS.split("&"); 
			for (var i = 0; i < pairs.length; i++) 
			{ 
				if (pairs[i].indexOf("search=") == 0 ) 
				{ 
					var pL=pairs[i].length; 
					var sekw = pairs[i].substring(7,pL); 
					msg+="&if_nt_SearchEngine_KW=" + sekw.toUpperCase();
				}
			} 
		}
	}
}
var msg2='';
if(self.location)
{
	var sQ='';
	sPath=self.location.href;
	sInd=sPath.indexOf( "?" );
	if( sInd!=-1 ) {sQ=sPath.substring(sInd+1,sPath.length);}
	if( sQ!='' ) 
	{
		sInd=sQ.indexOf("aff=");
		if( sInd!=-1 )
		{
			var sInd2=sQ.indexOf("&",sInd+4);
			if( sInd2==-1 ) { sInd2=sQ.length; }
			if_affiliate=sQ.substring(sInd+4,sInd2);
			msg2 += "&Affiliate=" + escape(if_affiliate);
			msg2 += "&if_Event=" + escape(if_affiliate);
		}
	}
}
self.document.insightxeimg=new Image();
self.document.insightxeimg.src=window.location.protocol+"//insightxe.artistdirect.com/data/?&tax0_SiteID=1" +  mep1 + msg + msg2;