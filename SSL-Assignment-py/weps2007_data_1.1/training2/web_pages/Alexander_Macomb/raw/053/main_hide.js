<!--No3 = (parseInt(navigator.appVersion) > 3) ? 1:0;layer = (document.all && No3) ? "document.all['L'+menu].style" : (document.layers && No3) ? "document.layers['L'+menu]" : (document.getElementById && !document.all) ? "document.getElementById('L'+menu).style" : 0;var timer;function Show() {   if(layer) {    if(timer) clearTimeout(timer);    for(menu=0; menu<Layer.length; menu++) {      if(Layer[menu]) { eval(layer).visibility = "hidden";	  for(i=1; i<=5; i++) {	    if (i == menu) {	        setImage("nav0" + i, eval("nav0" + i + "off"))	      }		}      }   }    for(i=0; i<arguments.length; i++) {    menu=arguments[i]; eval(layer).visibility = "visible";	for(t=1; t<=5; t++) {	    if (t == arguments[i]) {	        setImage("nav0" + t, eval("nav0" + t + "on"))	    }      }    }  } }function Hide() {  timer = setTimeout("Show()", 500);}//-->